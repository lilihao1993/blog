layui.define(['layer', 'form', 'element', 'laydate','validate'], function (exports) {
    var layer = layui.layer,
        element = layui.element(),
        form = layui.form();
    
    exports('client/user/noteEditor', {});
});